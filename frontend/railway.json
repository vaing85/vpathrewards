{
  "$schema": "https://railway.com/railway.schema.json",
  "deploy": {
    "buildCommand": "npm install && npm run build",
    "startCommand": "npx vite preview --port 4173 --host 0.0.0.0",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
